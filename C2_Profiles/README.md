# External C2 profiles

BlueShell uses separately installed profiles from
`github.com/MythicC2Profiles`.

This repository intentionally does not install or replace those services.
Install the required `http`, `httpx`, `smb`, or `tcp` profile independently.
